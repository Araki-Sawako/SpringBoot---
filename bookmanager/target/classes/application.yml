server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sampledb
    username: root
    password: password
  mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.college.yi.bookmanager.model
